// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase:{
    apiKey: "AIzaSyAqd-fV_ulX4TlQN-kJJEcs5qq8PKvlmEU",
    authDomain: "quickrdemo.firebaseapp.com",
    databaseURL: "https://quickrdemo.firebaseio.com",
    projectId: "quickrdemo",
    storageBucket: "quickrdemo.appspot.com",
    messagingSenderId: "421488526086",
    appId: "1:421488526086:web:39cb0355d489c849"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
